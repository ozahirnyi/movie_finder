# Use with docker-compose.lightsail.yml when nginx on the host listens on 80
# and proxies to the app. App will bind to host port 8000 only.
# Example: docker-compose -f docker-compose.lightsail.yml -f docker-compose.lightsail-nginx.yml up -d
services:
  web:
    ports:
      - "8000:8000"
