# Override: expose app on host port 80 (no nginx in front).
# Use: docker-compose -f docker-compose.lightsail.yml -f docker-compose.lightsail-direct.yml up -d
services:
  web:
    ports:
      - "80:8000"
